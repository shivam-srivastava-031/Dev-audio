# =====================================================================
# Book-to-Audio Pipeline — Dependencies
# =====================================================================

# --- Core ML / NLP ---
transformers>=4.40.0
datasets
evaluate
albumentations
jiwer                       # WER/CER metrics
accelerate
sentencepiece
torch>=2.0.0

# --- PDF processing ---
PyMuPDF                     # PDF → page images (fitz)

# --- OCR fine-tuning & training ---
Pillow

# --- Language detection & translation ---
langdetect

# --- Audio processing ---
pydub
ffmpeg-python
mutagen
pyloudnorm

# --- TTS (choose one or more) ---
TTS                         # Coqui XTTS-v2 (includes XTTS-v2)
gTTS                        # Google TTS fallback (no GPU)
# fish-speech               # Install separately: see Fish Speech docs

# --- LLM inference ---
ollama                      # Ollama Python client (for local Llama 3.1)
# Note: Llama via transformers requires: pip install bitsandbytes
# and a HuggingFace token for meta-llama/Llama-3.1-8B-Instruct

# --- Layout detection ---
# doclayout-yolo requires ultralytics:
ultralytics
huggingface-hub

# --- Config & utils ---
PyYAML
tqdm
numpy
requests
